spring:
  grpc:
    server:
      port: 9090
      reflection:
        enabled: true
      health:
        enabled: true
    client:
      channels:
        downstream-server:
          address: static://localhost:9091
